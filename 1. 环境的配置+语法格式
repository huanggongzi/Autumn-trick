java最重要的特点：网络传输和高并发

环境的配置

编码规范(UTF-8)

字的大小

test.java文件的创建

标识符

 以字母、美元符($)、下划线(_)、数字组成
 数字不能开头
 不能是关键字
     PS:标识符是大小写敏感的(见名识意)

命名规则
     包名:习惯使用小写字母拼写。(并列/嵌套)
     类文件名:首字母应该大写。若由若干单词组成，则每个单词首字母应该大写，如: MyFirstJavaClass
     方法、变量名:首字母应该小写。若由若干单词组成，则之后每个单词首字母大写。如: getName
     五级包名目录结果: com.公司名.项目名.模块名.层级名

关键字：

PS:在很多集成开发环境中，true false与nulI与关键字相同的特殊颜色给以标出，但不是关键字。在Java语言中，这3个符号是3个字面常量(也称直接量)。其中，true与false是布尔类型的字面常量，null是引用类型的字面常量。这些就好比“abc" 是String类型的字面常量，而数值“5”是int类型的字面常量- -样。尽管true false与null不是关键字，但是，也不能将其作为标识符使用，否则同样会产生编译错误。从这一点来说，这3个字面常量与关键字的表现是相似的。

注释

// 单行注释
/*
* 多行注释1
* 多行注释1
*/
/**
 * 接口、变量注释~
 * 
*/
Scanner类
   对于此类，需用 String string = scanner.nextLine();来获取输入，再用Integer.parseInt(string)转为整数
